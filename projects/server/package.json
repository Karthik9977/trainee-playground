{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "description": "JSON Server API",
  "author": "Shibashis Deb",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "start:delay": "json-server --watch db.json --port 3000 --delay 1000",
    "start:routes": "json-server --watch db.json --port 3000 --routes routes.json"
  },
  "keywords": [
    "json-server",
    "api",
    "mock"
  ],
  "license": "ISC",
  "dependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
